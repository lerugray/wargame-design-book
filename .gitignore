docs/_site/
docs/.sass-cache/
docs/.jekyll-cache/
docs/.jekyll-metadata
docs/Gemfile.lock
*.swp

# Claude Code — preserve any future SKILLs cross-machine
.claude/*
!.claude/skills/

# Claude Design raw output (preview HTML + React + screenshots) —
# final deliverables live at print/cover/*.svg + cover-decisions.md
print/cover/claude-design-output/
wargame design book cover.zip
wargame_design_book_cover.zip

# GeneralStaff bot worktree (created by engineer_command.sh, cleaned on exit)
.bot-worktree/

# Pandoc / KDP build output — generated artifacts, not source.
# Exception: the final interior PDF is tracked so it can be pulled on
# other machines for the KDP UI upload step (interior is the only
# regularly rebuilt artifact; cover PDFs/PNGs/SVGs already in this dir
# stay tracked too).
print/out/*
!print/out/wargame-design-book.pdf
!print/out/wargame-design-book-cover.pdf
!print/out/wargame-design-book-cover.png
!print/out/wargame-design-book-cover.svg
!print/out/wargame-design-book-cover-upload.svg
print/build/
*.pdf.bak

# Python bytecode caches (build-cover.py imports kdp_cover etc.)
__pycache__/
*.pyc

# GS state junction (gs-232 parallel-picker workaround)
state/


# 2026-04-24 overnight-bot sanity cleanup � untracked artifacts that were blocking GS dispatcher clean-tree preflight
wargame-book-full-audit-brief*.md

